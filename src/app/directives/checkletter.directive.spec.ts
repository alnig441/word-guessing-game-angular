import { CheckletterDirective } from './checkletter.directive';

describe('CheckletterDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckletterDirective();
    expect(directive).toBeTruthy();
  });
});
